name: Build

on:
  push:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: full

jobs:
  build:
    name: Build
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          repository: xuxiaocheng0201/dfg
          token: ${{ secrets.GH_TOKEN }}
          ref: 26f11a2f1f6a1a8cc3cea5282eb3404aa8675bd2

      - name: Check rust version
        run: rustup --version

      - name: Setup flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Check flutter version
        run: flutter --version

      - name: Cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            ./dfg_ui/rust/target/
            ./dfg_ui/.dart_tool/
            ./dfg_ui/build/
          key: dfg-ui-release-windows-x86_64

      - name: Install cargo tools
        uses: taiki-e/install-action@v2
        with:
          tool: flutter_rust_bridge_codegen@2.11.1,cargo-expand

      - name: Generate code
        working-directory: ./dfg_ui
        run: |
          flutter_rust_bridge_codegen integrate --no-enable-integration-test
          git reset --hard
          flutter pub get
          flutter_rust_bridge_codegen generate
          flutter create --platforms=windows .

      - name: Build release
        working-directory: ./dfg_ui
        run: |
          rustup run stable cargo build --manifest-path ./rust/Cargo.toml -p rust_lib_dfg_ui --release --target x86_64-pc-windows-msvc --target-dir ./build/windows/x64/plugins/rust_lib_dfg_ui/cargokit_build
          flutter build windows --release

      - name: Upload
        uses: actions/upload-artifact@v6
        with:
          path: dfg_ui/build/windows/x64/runner/Release
